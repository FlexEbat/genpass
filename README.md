
---

# 🔒 genpass.py —  Offline Password Generator

Не использует интернет, не сохраняет пароли на диск, использует системный CSPRNG (`secrets`) для максимально безопасной генерации.


---

## 📦 Требования

* Python 3.8+
* ОС: Linux / macOS / Windows
* Для буфера обмена на Linux: `xclip` или `xsel` (необязательно, только если используешь `--copy`)

---

## ⚠️ Основные принципы безопасности

* ❌ Пароли **не сохраняются на диск** и не логируются
* ❌ Не используется интернет
* ✅ Используется **CSPRNG** (`secrets`) для генерации
* ✅ Опциональная **очистка буфера обмена** через таймер
* ❗ Ответственность за хранение и резервное копирование паролей полностью на пользователе

---

## 🚀 Установка и запуск

Скачай скрипт и сделай исполняемым (необязательно):

```bash
chmod +x genpass.py
```

Запуск:

```bash
python3 genpass.py
```

* Если скрипт запускается без аргументов — открывается **интерактивный режим**.
* Можно запускать через `./genpass.py` на Linux/macOS, если файл исполняемый.

---

## 🔧 Основные режимы работы

### 1️⃣ Интерактивный режим

Запуск без аргументов:

* Пользователь вводит длину пароля
* Выбирает, какие категории символов включать:

  * Заглавные (A-Z)
  * Строчные (a-z)
  * Цифры (0-9)
  * Спецсимволы
* Можно включить “только безопасные” символы (без кавычек, обратных слэшей и т.п.)
* Скрипт проверяет:

  * Минимальную длину
  * Совместимость выбранных категорий с длиной

---

### 2️⃣ CLI / аргументы командной строки

#### Длина и количество

```bash
--len N      # Длина пароля (8-128)
--count N    # Количество паролей
```

#### Пресеты

```bash
--preset NAME
```

Доступные пресеты:

| Пресет | Длина | Состав           | Безопасные символы |
| ------ | ----- | ---------------- | ------------------ |
| web    | 16    | Все группы       | Да                 |
| strong | 32    | Все группы       | Нет                |
| pin    | 8     | Цифры            | Да                 |
| wifi   | 24    | Строчные + цифры | Да                 |

> Аргумент `--len` может переопределять длину пресета, а `--safe` — выбор безопасных символов.

#### Категории символов

```bash
--upper    # Заглавные A-Z
--lower    # Строчные a-z
--digits   # Цифры 0-9
--symbols  # Спецсимволы
--safe     # Только безопасные спецсимволы
--custom STR  # Собственный набор символов (перекрывает все флаги)
```

#### Форматирование и вывод

```bash
--group N  # Разбивка на группы по N символов
--sep CHAR # Разделитель групп (по умолчанию "-")
--quiet / -q  # Тихий режим, только пароли
```

#### Буфер обмена

```bash
--copy / -c    # Копировать последний (или все) пароли в буфер
--clear N      # Очистить буфер через N секунд
```

#### Самодиагностика

```bash
--selftest
```

* Проверяет генерацию и корректность категорий
* Проверяет длину пароля и кастомные наборы
* Возвращает exit-code: `0` — всё ок, `3` — ошибка

---

## 🔢 Оценка энтропии

После генерации выводится оценка стойкости:

| Энтропия (бит) | Стойкость |
| -------------- | --------- |
| ≥ 80           | 🔒 STRONG |
| 60–79          | ✅ OK      |
| < 60           | ⚠️ WEAK   |

> Энтропия рассчитывается по формуле: `log2(pool_size) * length`.

---

## 💡 Рекомендации по использованию

1. Генерируйте **уникальный пароль для каждого сервиса**
2. Используйте `--custom` для максимально контролируемых наборов символов
3. Сохраняйте пароли **только в зашифрованных файлах**:

   * 7zip (`7z a -pPASSWORD archive.7z passwords.txt`)
   * GPG (`gpg -c passwords.txt`)
4. Делайте резервные копии на **флешку или второй ПК**
5. Не оставляйте терминал открытым после генерации
6. Используйте таймер очистки буфера при копировании в clipboard

---

## 🧪 Примеры использования

**Генерация сильного пароля и копирование в буфер**

```bash
python3 genpass.py --preset strong --copy --clear 15
```

**Свой набор символов**

```bash
python3 genpass.py --custom "abcdef123" --len 32
```

**Несколько паролей сразу**

```bash
python3 genpass.py --len 20 --count 5
```

**Разбивка на группы для удобства**

```bash
python3 genpass.py --len 16 --group 4 --sep ":"
# Вывод: abcd:EF12:xyZ9:MnOp
```

**Тихий режим**

```bash
python3 genpass.py --len 32 -q
```

**Запуск самодиагностики**

```bash
python3 genpass.py --selftest
```

---

## ⚡ Exit-codes

| Код | Значение                                              |
| --- | ----------------------------------------------------- |
| 0   | Success                                               |
| 1   | Ошибка аргументов                                     |
| 2   | Системная ошибка (например, буфер обмена не доступен) |
| 3   | Самодиагностика не пройдена                           |

---

## 📚 FAQ / Примечания

**Q:** Почему не использовать онлайн генератор?
**A:** Пароли могут остаться в браузере, кэше, истории форм. Офлайн CSPRNG безопаснее.

**Q:** Можно ли хранить пароли в `.txt`?
**A:** Да, но **обязательно шифруйте** через 7zip / GPG.

**Q:** Можно ли отключить все категории?
**A:** Нет, хотя бы одна категория должна быть выбрана, иначе генерация невозможна.

**Q:** Что делать, если буфер обмена не работает?
**A:** На Linux установите `xclip` или `xsel`, на Windows/macOS стандартные утилиты уже есть.

---

